- if Like.find_by(user_id: current_user.id, talk_id: talk.id)
  = link_to talk_like_path(talk, talk.likes), method: :delete, remote: true do
    = icon 'fas', 'star'
- else
  = link_to talk_likes_path(talk), method: :post, remote: true do
    = icon 'far', 'star'
= talk.likes.count